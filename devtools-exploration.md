# DevTools Exploration

## Website 1: https://example.com

- **Page title:** Example Domain  
- **HTML tags used:**  
  `<html>`, `<head>`, `<title>`, `<body>`, `<h1>`, `<p>`, `<a>`  
- **Number of headings:** 1  

---

## Website 2: https://developer.mozilla.org

- **Navigation menu tag:** `<nav>`  
- **Search bar structure:**  
  A `<form>` element containing an `<input type="search">` and a `<button>`  
- **Hover effect on links:**  
  When hovering over links, the text changes color and becomes underlined (checked using DevTools `:hover` state)

---

## Website 3: https://www.wikipedia.org

- **5 HTML elements identified:**  
  `<header>`, `<main>`, `<footer>`, `<section>`, `<img>`  

- **Form element and inputs:**  
  - Search input (`<input type="search">`)  
  - Language selector (`<select>`)  
  - Submit button (`<button>`)

---

## Screenshots

- Screenshot of Elements panel for Example.com  
- Screenshot of Elements panel for MDN  
- Screenshot of Elements panel for Wikipedia  

*(Screenshots should show the HTML structure inspected using browser DevTools)*
